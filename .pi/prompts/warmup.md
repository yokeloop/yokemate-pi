---
description: Print the pool digest on demand — queue, live work/ folders, journal tail — the same one the SessionStart hook injects at session start.
---

Read `.pi/skills/warmup/SKILL.md` in full — the path is relative to the yokemate root, which is this session's cwd — and follow it.
