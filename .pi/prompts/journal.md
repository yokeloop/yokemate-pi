---
description: Append a concise, newest-first entry about the session's real work to the pool-wide journal, one file per month, each entry naming its project.
argument-hint: "[что записать]"
---

Read `.pi/skills/journal/SKILL.md` in full — the path is relative to the yokemate root, which is this session's cwd — and follow it. What to record: $@
