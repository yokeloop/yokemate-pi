# yokemate

Оркестраторский чат для разработки по многим организациям и репозиториям: обычная сессия pi в корне этого репозитория, открытая в `herdr`, плюс семь скиллов и полтора десятка команд вокруг неё. Тикеты — из YouTrack, состояние — в SQLite; `/do` и `/ship` работают фоновыми coordinator-ами, а разговорные режимы — в собственных панелях.

## Быстрый старт

```bash
pnpm on-me                                        # тикеты на мне, по всем YouTrack
pnpm add-project <клон> --tracker acme:ACME --model openai-codex/gpt-5.6-terra   # подключить репозиторий
```

Дальше в главном чате: `/plan ACME-347` → `/do` → `/review` → `pnpm accept`. Пять интерактивных режимов — `/plan`, `/review`, `/worklog`, `/note`, `/research` — открывают новый tab по умолчанию. `--split` до первого `--` открывает split: `/plan --split ACME-347`; `/split plan ACME-347` — compatibility alias. `--tab` нет; после `--` параметры становятся буквальным текстом. Панель закрывает инженер. `/do` и `/ship` остаются фоновыми, мерж — только по явному `/ship`. Исследование без тикета `/research <project/topic>` сохраняет отдельное one-edit consent на каждую правку кода.

## Документы

- [`AGENTS.md`](./AGENTS.md) — правила и команды оркестратора; их читают агенты.
- [`docs/usage.md`](./docs/usage.md) — инструкция инженера: от подключения проекта до приёмки.
- `home/knowledge/<org>/<project>/` — глоссарии, ADR и артефакты задач; `home/journal/` — журнал пула.
